{% from 'virtual-mcp-pike-dvr/underlay.yaml' import HOSTNAME_CFG01 with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import HOSTNAME_CMP01 with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import HOSTNAME_CMP02 with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import HOSTNAME_GTW01 with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import HOSTNAME_CTL01 with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import HOSTNAME_CTL02 with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import HOSTNAME_CTL03 with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import HOSTNAME_PRX01 with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import HOSTNAME_DNS01 with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import HOSTNAME_DNS02 with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import HOSTNAME_SHARE01 with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import LAB_CONFIG_NAME with context %}
{% from 'virtual-mcp-pike-dvr/underlay.yaml' import DOMAIN_NAME with context %}

{% set SALT_MODELS_REPOSITORY = os_env('SALT_MODELS_REPOSITORY','https://gerrit.mcp.mirantis.net/salt-models/mcp-virtual-lab') %}
# Other salt model repository parameters see in shared-salt.yaml

{% import 'shared-salt.yaml' as SHARED with context %}

{{ SHARED.MACRO_INSTALL_SALT_MASTER() }}

{{ SHARED.MACRO_CLONE_RECLASS_MODELS() }}

{{ SHARED.MACRO_CONFIGURE_RECLASS(FORMULA_SERVICES='"linux" "reclass" "salt" "openssh" "ntp" "git" "nginx" "collectd" "sensu" "heka" "sphinx" "keystone" "mysql" "grafana" "haproxy" "rsyslog" "horizon" "prometheus" "telegraf" "elasticsearch" "powerdns" "fluentd" "backupninja" "runtest" "neutron" ') }}

{{ SHARED.MACRO_INSTALL_SALT_MINIONS() }}

{{ SHARED.MACRO_RUN_SALT_MASTER_UNDERLAY_STATES() }}

{{ SHARED.MACRO_GENERATE_INVENTORY() }}

{{ SHARED.MACRO_BOOTSTRAP_ALL_MINIONS() }}

{{SHARED.MACRO_CHECK_SALT_VERSION_SERVICES_ON_CFG()}}
{{SHARED.MACRO_CHECK_SALT_VERSION_ON_NODES(HOSTNAME_CTL01)}}
{{SHARED.MACRO_CHECK_SALT_VERSION_ON_NODES(HOSTNAME_CTL02)}}
{{SHARED.MACRO_CHECK_SALT_VERSION_ON_NODES(HOSTNAME_CTL03)}}
{{SHARED.MACRO_CHECK_SALT_VERSION_ON_NODES(HOSTNAME_CMP01)}}
{{SHARED.MACRO_CHECK_SALT_VERSION_ON_NODES(HOSTNAME_CMP02)}}
{{SHARED.MACRO_CHECK_SALT_VERSION_ON_NODES(HOSTNAME_GTW01)}}
{{SHARED.MACRO_CHECK_SALT_VERSION_ON_NODES(HOSTNAME_PRX01)}}
{{SHARED.MACRO_CHECK_SALT_VERSION_ON_NODES(HOSTNAME_SHARE01)}}
{{SHARED.MACRO_CHECK_SALT_VERSION_ON_NODES(HOSTNAME_DNS01)}}
{{SHARED.MACRO_CHECK_SALT_VERSION_ON_NODES(HOSTNAME_DNS02)}}
