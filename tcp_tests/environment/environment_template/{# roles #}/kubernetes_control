{#-
parameters:
  reclass:
    storage:
      node:
        <reclass_storage_node_name>:
          classes:
#}
          - cluster.${_param:cluster_name}.kubernetes.control
{%- set _ = params.update({'keepalived_vip_interface': 'ens3'}) %}
{%- set _ = local_metadata.update({'keepalived_vip_priority': True}) %}